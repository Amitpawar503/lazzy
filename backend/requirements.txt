# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Data
httpx>=0.27
pandas>=2.2
numpy>=1.26

# Optional India data sources (graceful-degrade if missing/offline)
yfinance>=0.2.40 ; python_version >= "3.9"
jugaad-data>=0.26 ; python_version >= "3.9"

# Caching (optional; falls back to in-memory)
redis>=5.0
